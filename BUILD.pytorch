config_setting(
    name = "using_cuda",
    values = {"define": "using_cuda=true"},
)

config_setting(
    name = "using_rocm",
    values = {"define": "using_rocm=true"},
)

cc_library(
    name = "torch",
    srcs = [
        "torch_root/lib/libtorch.so",
        "torch_root/lib/libtorch_python.so",
        "torch_root/lib/libtorch_cpu.so",
        "torch_root/lib/libshm.so",
        "torch_root/lib/libc10.so",
    ] + glob([
        "torch_root/lib/libgomp*.so*",
    ]) + select({
        "@//:using_cuda": [
            "torch_root/lib/libtorch_cuda.so",
            "torch_root/lib/libc10_cuda.so",
        ] + glob([
            "torch_root/lib/libcudart*.so*",
            "torch_root/lib/libnvToolsExt*.so*",
        ]),
        "@//:using_rocm": [
            "torch_root/lib/libcaffe2_nvrtc.so",
            "torch_root/lib/libtorch_hip.so",
            "torch_root/lib/libc10_hip.so",
        ] +  glob([
            "torch_root/lib/libaotriton_v2.so*",
        ]),
        "//conditions:default": [],
    }),
    hdrs = glob([
        "torch_root/include/*.h",
        "torch_root/include/**/*.h",
        "torch_root/include/**/*.cuh",
        "torch_root/include/**/*.hpp",
    ], exclude=["torch_root/include/google/**"]),
    deps = [
        "@local_config_python//:python_headers",
        "@local_config_python//:python_lib",
    ],
    strip_include_prefix = "torch_root/include",
    visibility = ["//visibility:public"],
)

cc_library(
    name = "torch_api",
    hdrs = glob([
        "torch_root/include/torch/csrc/api/include/**/*.h",
    ]),
    deps = [
        "@local_config_python//:python_headers",
    ],
    strip_include_prefix = "torch_root/include/torch/csrc/api/include",
    visibility = ["//visibility:public"],
)

cc_library(
    name = "torch_libs",
    srcs = glob([
        "torch_root.libs/libarm_compute*.so",
        "torch_root.libs/libgfortran*.so*",
        "torch_root.libs/libgomp*.so*",
        "torch_root.libs/libopenblasp*.so",
    ]),
    linkopts = select({
        "@//:cuda_pre_12_9": [
            "-L/usr/local/cuda/lib64",
            "-L/usr/local/cuda/extras/CUPTI/lib64",
            "-lcupti",
            "-lcudart",
            "-lnccl",
            "-lcudnn",
            "-lnvToolsExt",
        ],
        "@//:using_cuda12_9_x86": [
            "-L/usr/local/cuda/lib64",
            "-L/usr/local/cuda/extras/CUPTI/lib64",
            "-lcupti",
            "-lcudart",
            "-lnccl",
            "-lcudnn",
            "-lnvtx3interop",
        ],
        "//conditions:default": [],
    }),
    deps = [
        "@local_config_python//:python_headers",
    ],
    visibility = ["//visibility:public"],
)
