-r requirements_base.txt
apache-tvm-ffi
auto-gptq
autoawq
deep-ep==1.2.1.10+d7d7b48 ; platform_machine == "x86_64"
deep-gemm==2.1.1+local ; platform_machine == "x86_64"
fast-hadamard-transform==1.0.4.post1 ; platform_machine == "x86_64"
fast-safetensors==0.7.3+torch2.1.2.cu121 ; platform_machine == "x86_64"
fastsafetensors==0.1.19rc5+ali ; platform_machine == "x86_64"
flash-attn==2.8.3+cu12torch2.8cxx11abiTRUE ; platform_machine == "x86_64"
flash-attn-3==3.0.0b1
flash-mla==1.0.0+ca58fed ; platform_machine == "x86_64"
flashinfer-cubin==0.6.9
flashinfer-jit-cache==0.6.9
flashinfer-python==0.6.9
mysql-connector-python
nvidia-cutlass-dsl==4.4.2
# rtp-kernel is built per-arch, so this x86_64 wheel carries a different build
# timestamp than the aarch64 one in requirements_cuda12_arm.txt. Keep both pins
# on the SAME source commit (currently 125c29e5) and bump + regenerate both
# lockfiles together, otherwise a one-sided regen would silently ship mismatched
# rtp-kernel builds across architectures.
rtp-kernel==0.1.0+125c29e5.20260422154605 ; platform_machine == "x86_64"
tabulate==0.9.0
tensorrt==10.3.0 ; platform_machine == "x86_64"
tensorrt-cu12-bindings==10.3.0 ; platform_machine == "x86_64"
tensorrt-cu12-libs==10.3.0 ; platform_machine == "x86_64"
tilelang==0.1.6
torch==2.8.0+cu129
torchvision==0.23.0+cu129
