# Configuration for the registered Python case; execution and assertions live in Python.
schema_version: 2
case: balance_overload_transfer
environment:
  backend: java_mock
  n_prefill: 2
  n_decode: 4
execution:
  timeout_s: 1800
  stage_timeout_s: 60
  cleanup_timeout_s: 120
variants:
- id: decode_pressure
- id: prefill_pressure
