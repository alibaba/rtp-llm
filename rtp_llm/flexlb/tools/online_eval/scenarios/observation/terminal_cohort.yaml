# Configuration for the registered Python case; execution and assertions live in Python.
schema_version: 2
case: observed_terminal_cohort
environment:
  backend: java_mock
  n_prefill: 2
  n_decode: 2
execution:
  timeout_s: 300
  stage_timeout_s: 60
  cleanup_timeout_s: 120
variants:
- id: deferred
  profiles:
  - batch-window
  - single-batch
